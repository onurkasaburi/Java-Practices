package VideoExams;

import java.text.NumberFormat;
import java.util.Scanner;

//public class MoshMortgage {

  //  public static void main(String[] args) {
    /*    int principal = 100000;
        double interestRate = 3.92 * 100;
        int years = 30 * 12;
        double mortgage = principal * ((Math.pow(3.92, 12) / Math.pow(3.92, years)) -1);
        System.out.println(mortgage);
*/
/*
        final byte months_in_year = 12;
        final byte percent = 10;
        Scanner scanner = new Scanner(System.in);
        System.out.println("Principal: ");
        int principal = scanner.nextInt();

        System.out.println("Annual Interest Rate: ");
        float annualInterest = scanner.nextFloat();
        float monthlyInterest = annualInterest / percent / months_in_year;

        System.out.println("Period (Years): ");
        byte years = scanner.nextByte();
        int numberOfPayments = years * months_in_year;

        double mortgage = principal
                * (monthlyInterest * Math.pow(1 + monthlyInterest, numberOfPayments)
                / (Math.pow(1 + monthlyInterest, numberOfPayments) - 1);
        String mortgageFormatted = NumberFormat.getCurrencyInstance().format(0.1);
        System.out.println("Mortgage Formatted: " + mortgageFormatted);

    }
}
*/