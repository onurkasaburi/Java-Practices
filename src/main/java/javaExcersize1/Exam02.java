package javaExcersize1;

import java.util.HashMap;
import java.util.Map;
/*
public class Exam02 {
    //  5.Write a method which returns how many times each letter is repeated?


    public static Map<String, Integer> lettersCount(String str) {
        Map<String, Integer> map = new HashMap<>();
        String[] strMap = str.split("");

        for (String each : strMap) {
            if (!map.containsKey(str)) {
                map.put(each, 1);
            } else {
                map.put(each, each.length() + 1);
            }
            return map;
        }
    }
*/
/*

    public static void main(String[] args) {

        public static Map<String, Integer> lettersCount(String str){
            Map<String, Integer> map = new HashMap<>();
           // str = "izzmirimmm";
            String[] strMap = str.split("");

            for (String each : strMap) {
                if (!map.containsKey(str)) {
                    map.put(each, 1);
                } else {
                    map.put(each, each.length() + 1);
                }
                return map;
            }
        }
    }

*/
